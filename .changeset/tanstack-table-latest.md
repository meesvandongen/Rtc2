---
'@mvd/table': patch
---

Upgrade `@tanstack/react-table` from the `9.0.0-beta.58` prerelease to the
stable `9.1.2`, and `@tanstack/react-virtual` from `3.14.8` to `3.14.9`.
No API changes were required — the beta's row-model and pagination APIs this
package relies on carried through to the stable release unchanged.
