import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Docs/02 Features" />

# Features

Each of these has a dedicated story section in the sidebar.

| Area | Options |
| --- | --- |
| Sorting | `enableSorting`¹, `enableMultiSort`¹, `enableSortingRemoval`¹, `sortDescFirst`, `maxMultiSortColCount`, `manualSorting`, per-column `sortFn` |
| Column filtering | `enableColumnFilters`¹, `filterDisplayMode` (`popover` \| `panel` \| `popover-and-panel` \| `none`), `filterPanelPosition`, `enableFilterModes`, `showActiveFilterChips`¹, `manualFiltering`, `enableMultipleFilterConditions`, `dataTypes`, `filterNow`, 9 built-in [data types](?path=/docs/docs-05-filter-data-types--docs), `enableMobileFilterDrawer`¹, `mobileBreakpoint` |
| Global filtering | `enableGlobalFilter`¹, `globalFilterFn`, `enableGlobalFilterModes`, `globalFilterModeOptions`, `enableGlobalFilterToggle`¹ |
| Faceting | `enableFaceting`¹ — auto-populates select/autocomplete/checkbox filter options |
| Pagination | `enablePagination`¹, `paginationDisplayMode`, `paginationPosition`, `pageSizeOptions`, `manualPagination`, `rowCount`, `pageCount`, `autoResetPageIndex` |
| Row selection | `enableRowSelection` (bool or predicate), `enableMultiRowSelection`¹, `enableSubRowSelection`¹, `enableSelectAll`¹, `selectDisplayMode` (`checkbox` \| `radio` \| `switch`), `enableClickToSelect` |
| Cell selection | `enableCellSelection`, `enableCellRangeSelection`, `enableMultiCellRangeSelection` |
| Column visibility | `enableColumnVisibility`¹, `enableHiding`¹ |
| Column ordering | `enableColumnOrdering`, `enableColumnDragging` |
| Column pinning | `enableColumnPinning` — start/end, sticky with an edge border (`--rtc-border-pin`) |
| Row pinning | `enableRowPinning` (bool or predicate) — pin/unpin from each row's overflow menu, `rowPinningDisplayMode` (`sticky`, which keeps a row in the order and docks it at whichever edge it would leave, \| `top` \| `bottom` \| `top-and-bottom`, which lift rows into a section), `keepPinnedRows`¹ — a pinned row survives the filter or page that drops it |
| Column resizing | `enableColumnResizing`, `columnResizeMode`, `columnResizeDirection`, keyboard-operable grips |
| Grouping | `enableGrouping`, `enableGroupingChips` (drag-to-group), `groupedColumnMode` |
| Aggregation | `enableAggregation`¹, per-column `aggregationFn` + `aggregatedCell` |
| Expanding | `enableExpanding`, `enableExpandAll`¹, `getRowCanExpand`, `paginateExpandedRows`¹, `renderDetailPanel` |
| Row utilities | `enableRowNumbers`, `rowNumberDisplayMode`, `enableRowActions`, `renderRowActions`, `rowActionMenuItems`, `positionActionsColumn`, `enableRowOrdering` |
| Editing | `enableEditing` (bool or predicate), `editMode` (`cell` \| `row` \| `table` \| `modal`), 5 editor variants, `onEditingRowSave`, `onCellEditComplete`, `onDataChange` |
| Virtualization | `enableRowVirtualization`, `enableColumnVirtualization`, `rowVirtualizerOptions`, `columnVirtualizerOptions` — see [Virtualization](?path=/docs/docs-08-virtualization--docs) |
| Layout | `layoutMode` (`semantic` \| `grid` \| `grid-no-grow`), `density`, `height`, `maxHeight`, `direction` (LTR/RTL) |
| Orientation | `transposed`, `enableTransposeToggle` — one row per column, one column per record, every feature turning with it; see [Transposed](?path=/docs/docs-13-transposed--docs) |
| Header sizing | `enableHeaderContentFit`¹ — a column is never narrower than its own header |
| Chrome | `enableTopToolbar`¹, `enableBottomToolbar`¹, `enableToolbarInternalActions`¹, `enableTableHead`¹, `enableTableFooter`¹, `enableDensityToggle`¹, `enableFullScreenToggle`¹, `enableColumnActions`, `enableStickyHeader`, `enableStickyFooter`, `enableStripes`, `enableRowHover`¹, `enableBorders` |
| Toolbar layout | `toolbarLayout`, `toolbarItems` — where each occupant sits, and content of your own to place beside them |
| States | `isLoading`, `showProgressBars`, `isSaving`, `isLoadingError`, `errorMessage`, `skeletonRowCount`, `renderEmptyState` |
| i18n | [`localization`](?path=/docs/docs-11-localization--docs) — every string, including filter operator names, per data type where they differ |
| Clipboard | `enableClickToCopy`, or `meta.enableClickToCopy` per column |
| Escape hatches | `components`, `classNames`, `cssVars`, `tableProps`, `containerProps`, `rowProps`, `cellProps`, `headCellProps`, `renderCaption` — and `renderTopToolbarActions`, `renderBottomToolbarActions`, `renderToolbarInternalActions`, deprecated in favour of `toolbarItems` |

¹ on by default; everything else is opt-in.

## Chrome that removes itself

The four bands around the rows — the top toolbar, the column header, the
column footer and the bottom toolbar — each leave nothing behind when they are
switched off, and the two toolbars go one step further: a toolbar with no
occupant is not drawn at all, since padding and a divider on their own are just
a stray sliver of chrome. Pagination is all the bottom bar holds by default, so
`enablePagination={false}` clears the bar with it; the top bar goes the same way
once the search box, the internal actions and every chip are gone.

Either bar reappears the moment it has something to say — a selection count, an
active filter chip — so pass `enableTopToolbar={false}` when a table must keep
its height whatever the rows are doing. `enableTableFooter` only ever has
anything to hide when a column declares a `footer`; with none, there is no
`<tfoot>` to begin with. The
[Chrome story](?path=/story/datatable-01-basics--chrome) shows each choice on
its own table.

## Where the toolbar's occupants sit

Each bar has a `start`, a `center` and an `end`, and `toolbarLayout` says what
goes in each of them, in order.

```tsx
<DataTable
  toolbarLayout={{
    top: { start: ['search'] },
    bottom: { end: ['pagination', 'export'] },
  }}
  toolbarItems={{ export: ({ table }) => <ExportButton table={table} /> }}
/>
```

**A region you write is exactly what that region holds.** So the layout above
says the top bar starts with the search box and nothing else, and the bottom
bar ends with the pagination and then a button — while the regions it does not
mention, the top `end` among them, keep their defaults: the search toggle, the
columns menu and the rest of the icon cluster are all still there.

That is also how a layout takes something away. There is no separate verb for
it, and nothing to keep in step with a list of placements:

```tsx
// the columns menu and the density toggle, and nothing else in that region
toolbarLayout={{ top: { end: ['search', { group: ['column-visibility', 'density-toggle'] }] } }}

// no top-right corner at all
toolbarLayout={{ top: { end: [] } }}
```

A region with nothing in it is not drawn, and a bar left with no regions goes
with it — `{ bottom: { start: [], end: [] } }` removes the bottom bar the same
way emptying it of occupants does.

### Every built-in item

Fifteen ids, plus whatever you register in `toolbarItems`. "Drawn when" is the
existing option that decides whether the item exists at all — a layout places
what a table is already drawing and never brings back something a flag turned
off, though it can leave out something a flag turned on.

| id | What it is | Default place | Drawn when |
| --- | --- | --- | --- |
| `top-actions` | your own content for the top bar | `top` `start` | registered in `toolbarItems` |
| `grouping-chips` | the drag-to-group drop zone | `top` `start` | `enableGrouping` + `enableGroupingChips` |
| `selection-summary` | "3 of 50 rows selected" | `top` `start` | `enableRowSelection`, and a row is selected |
| `filter-chips` | a removable chip per active column filter | `top` `start` | `enableColumnFilters`¹ + `showActiveFilterChips`¹, and a filter is set |
| `search` | the global search field | `top` `end` | `enableGlobalFilter`¹, and the toggle is on or `enableGlobalFilterToggle={false}` |
| `pagination` | the pagination nav | `top` and/or `bottom` `end`, per `paginationPosition` | `enablePagination`¹ |
| `internal-actions` | your own content for the icon cluster | `top` `end`, leading the cluster | registered in `toolbarItems` |
| `search-toggle` | show/hide the search field | `top` `end`, in the cluster | `enableGlobalFilter`¹ + `enableGlobalFilterToggle`¹ |
| `filter-toggle` | open the filter panel, or the sheet on a phone | `top` `end`, in the cluster | `enableColumnFilters`¹, and there is a panel or sheet to open |
| `column-visibility` | the show/hide columns menu | `top` `end`, in the cluster | `enableColumnVisibility`¹ |
| `density-toggle` | cycle comfortable / compact / spacious | `top` `end`, in the cluster | `enableDensityToggle`¹ |
| `transpose-toggle` | flip the axes | `top` `end`, in the cluster | `enableTransposeToggle`, and `transposed` is unset |
| `fullscreen-toggle` | enter and leave full screen | `top` `end`, in the cluster | `enableFullScreenToggle`¹ |
| `error` | the loading-error alert | `top` `end`, in the cluster | `isLoadingError` |
| `bottom-actions` | your own content for the bottom bar | `bottom` `start` | registered in `toolbarItems` |

¹ on by default. Every id in the cluster also needs
`enableToolbarInternalActions`¹, which turns the whole group off at once.

Four things follow from a region being a list rather than a position:

- **An id named twice is drawn twice.** `{ top: { end: ['pagination'] },
  bottom: { end: ['pagination'] } }` is what `paginationPosition="both"` means,
  and naming it in one bar alone gives up its seat in the other.
- **A bar takes an array of rows.** `top: [{}, { start: ['filter-chips'] }]`
  keeps the bar as it was — the first row writes no region, so every region
  inherits — and gives the chips a line of their own underneath, where a long
  filter cannot squeeze the search box. Only that first row inherits; a row you
  add starts empty.
- **A cluster is an object.** `{ group: ['density-toggle', 'fullscreen-toggle']
  }` sits at the icon gap rather than the toolbar's, and wraps as one. That is
  what the built-in icon actions are, which is why pulling one of them out
  leaves the rest clustered.
- **`rest` puts back what a rewritten region displaced.** `{ top: { start:
  ['search', 'rest'] } }` moves the search box to the front and keeps whatever
  else the start held, rather than replacing it. Anything of your own that the
  layout never names goes there too, and each arrives in the shape it had, so
  a cluster moved through `rest` is still a cluster. Register an item and never
  name it anywhere and it lands at the end of the top bar.

Below `mobileBreakpoint`, `toolbarLayout.narrow` replaces whichever bars it
names, since the arrangement a phone wants is rarely the wide one with a couple
of things moved.

### Your own content

`toolbarItems` is the way in, and it **replaces the three
`render*ToolbarActions` slots**, which are deprecated. Each of those appends to
one fixed point — `renderToolbarInternalActions` could only ever lead the icon
cluster — where an item registered here can be named in `toolbarLayout` and put
anywhere, twice if you like.

The three ids those slots filled are registerable, and keep the places the
slots had, so migrating is the key and nothing else:

```tsx
// before
renderTopToolbarActions={({ table }) => <Bulk table={table} />}

// after — same place, and now movable
toolbarItems={{ 'top-actions': ({ table }) => <Bulk table={table} /> }}
```

An entry in `toolbarItems` wins over the slot of the same name. A value can be
a node or a function of the render context, as the slots were.

Nothing empty is ever drawn: a region with no occupants is not rendered, a row
with no regions goes with it, and a bar left with no rows goes the same way the
emptied bars above do. The
[Toolbar layout story](?path=/story/datatable-01-basics--toolbarlayout) shows
each arrangement on its own table.
